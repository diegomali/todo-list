{
  "name": "purescript-todo-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn build && parcel index.html",
    "install": "spago install",
    "build-production": "yarn build && parcel build index.html",
    "build": "spago build",
    "clean": "rm -rf .cache .spago .psci_modules node_modules output dist"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "todomvc-app-css": "^2.4.1",
    "todomvc-common": "^1.0.5"
  },
  "devDependencies": {
    "parcel": "^2.0.1",
    "purescript": "^0.14.5",
    "spago": "^0.20.3"
  }
}
